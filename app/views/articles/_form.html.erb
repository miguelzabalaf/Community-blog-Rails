<div class="container">
  <%= simple_form_for @article, local: true do |f| %>
  <div class="form-group">
    <%= f.input :title, placeholder: "Minimun 8, máximun 75 characters" %>
  </div>
  <div class="form-group">
    <%= f.input :body, class: 'form-control', placeholder: "Minimun 10 characters" %>
  </div>

  <%= f.hidden_field :user_id, value: current_user.id %>

  <div class="actions">
    <%= f.button :submit, class:"btn btn-primary btn-sm" %>
    <%= link_to 'Cancel', articles_path, class:"btn btn-danger btn-sm ml-2"%>
  </div>
  <% end %>
</div>